# 仅在 `npm run dev:dummy`（=`vite --mode dummy`）下加载。
# 客户端代码用 `import.meta.env.VITE_DUMMY_AUTH === 'true'` 读取，dev server 启动时由
# Vite 注入。prod build 时 `vite.config.ts` 会主动 throw，禁止进 bundle。
# 2026-08-28 新增：替代旧的 `define: { __DUMMY_AUTH__ }` —— Vite 8 的 define 插件
# 在 dev client 不做裸全局替换（见 docs/08-known-risks/framework-pitfalls.md 第 6 节），
# dev-only 开关必须走 `import.meta.env` + mode/env 文件。
VITE_DUMMY_AUTH=true
